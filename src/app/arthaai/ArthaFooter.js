// Unused — ArthaAI now uses the site-wide Header/Footer (via SiteChrome),
// not its own separate footer. Kept as an empty placeholder rather than
// deleted since this tool can't remove files; safe to delete manually.
export default function ArthaFooter() {
  return null;
}
